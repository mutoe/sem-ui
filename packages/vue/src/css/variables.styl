:root {
  // Colors
  --color-text-default: rgba(#000, 0.87);

  // Font size
  --font-size-basic: 14px
}

$color-text-default = var(--color-text-default, rgba(#000, 0.87))

$font-size-md = var(--font-size-basic, 14px)
